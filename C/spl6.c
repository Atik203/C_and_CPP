#include <stdio.h>

int main()
{
    int n, i, j, sum = 0;

    scanf("%d", &n);
    int first = -j;
    for (int k = 1; k < n; k++)
    {
        sum += (k * i + (k - 1) * j);
    }
     sum = sum + first ;

    if (sum % 2 == 0)
    {
        printf("Yes,Your id.\n");
    } else {
        printf("No, Your name.\n");
    }

    return 0;
}
