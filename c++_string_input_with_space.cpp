#include<bits/stdc++.h>
using namespace std;
int main()
{ 
    int a;
    cin>>a;
     cin.ignore(); // or use  getchar();
   string s;
   // s.getline(s,100); this works for char s[100];
   getline(cin,s);
   cout<<a<<endl;
   cout<<s<<endl;
 
    return 0;
}